# 1. WebSocket 支持
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

# 2. 端口到协议的自动推断
map $raw_port $inferred_proto {
    "80"      "http";
    "8096"    "http";
    "8097"    "http";
    "8880"    "http";
    "2333"    "http";
    default   "https";
}

# 3. 客户端验证 (仅允许特定 Emby 客户端)
map $http_user_agent $is_emby_client {
    default 0;
    "~*(Hills|yamby|Afuse|Capy)" 1;
}

# HTTP 强制跳转 HTTPS
server {
    listen {{HTTP_PORT}};
    listen [::]:{{HTTP_PORT}};
    server_name {{SERVER_NAME}};
    location / { return 301 https://$host$request_uri; }
}

server {
    listen {{HTTPS_PORT}} ssl http2;
    listen [::]:{{HTTPS_PORT}} ssl http2; 
    server_name {{SERVER_NAME}};

    {{WHITELIST}}

    # SSL 配置
    ssl_certificate {{SSL_CERTIFICATE}};
    ssl_certificate_key {{SSL_CERTIFICATE_KEY}};
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_session_cache shared:SSL:20m;
    ssl_session_timeout 1h;
    ssl_session_tickets on;

    # 性能基础优化
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    client_max_body_size 0;
    
    # Gzip 压缩
    gzip on;
    gzip_types application/json text/plain text/css text/javascript application/javascript application/xml image/svg+xml;

    # DNS 解析器
    resolver 1.1.1.1 8.8.8.8 valid=300s ipv6=off;
    resolver_timeout 5s;

    # ------------------ 核心处理逻辑 ------------------

    location ~* ^/(?:(?<raw_proto>https?|wss?)://)?(?<raw_target>[a-zA-Z0-9\.-]+)(?:(?:_|:)(?<raw_port>\d+))?(?<raw_path>/.*)?$ {
        # 1. 安全拦截
        if ($is_emby_client = 0) { rewrite ^ /static-404 last; }

        # 2. 动态目标构建
        set $target_proto $inferred_proto;
        if ($raw_proto != "") { set $target_proto $raw_proto; }
        
        set $target_port "";
        if ($raw_port != "") { set $target_port ":$raw_port"; }
        
        set $final_path $raw_path;
        if ($final_path = "") { set $final_path "/"; }

        set $target_url "$target_proto://$raw_target$target_port$final_path$is_args$args";

        # 3. 代理核心设置
        proxy_pass $target_url;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        
        # ✅ 兼容前后端分离的关键：保留原始 Header 但隐藏反代 IP
        proxy_pass_request_headers on;
        proxy_set_header X-Real-IP "";
        proxy_set_header X-Forwarded-For "";
        proxy_set_header CF-Connecting-IP "";
        proxy_set_header X-Forwarded-Proto $scheme;

        # 4. SSL 握手修复 (解决 SNI 问题)
        proxy_ssl_server_name on;
        proxy_ssl_name $raw_target;
        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_ssl_verify off;
        
        # 5. Host 头部重写
        proxy_set_header Host $raw_target;

        # ✅ 核心增强：动态重定向处理
        # 解决分离架构中，主站返回推流站 302 导致客户端直接暴露真实 IP 的问题
        proxy_redirect ~*^https?://(?<redir_host>[a-zA-Z0-9\.-]+)(?::(?<redir_port>\d+))?(?<rel_path>.*)$ https://$host/$target_proto://$redir_host:$redir_port$rel_path;
        # 针对不带端口的情况进行特殊匹配
        proxy_redirect ~*^https?://(?<redir_host>[a-zA-Z0-9\.-]+)(?<rel_path>.*)$ https://$host/$target_proto://$redir_host$rel_path;

        # 6. 流媒体优化
        proxy_hide_header Content-Type;
        add_header Content-Type $upstream_http_content_type always;
        proxy_set_header Accept-Encoding "";
        
        proxy_buffering off;
        proxy_request_buffering off;
        proxy_set_header X-Accel-Buffering no;
        
        # 范围请求支持 (推流必备)
        proxy_force_ranges on;
        proxy_set_header Range $http_range;
        proxy_set_header If-Range $http_if_range;
        add_header Accept-Ranges "bytes" always;

        proxy_connect_timeout 15s;
        proxy_send_timeout 3600;
        proxy_read_timeout 3600;

        # 7. 跨域处理 (CORS)
        add_header 'Access-Control-Allow-Origin' '*' always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, DELETE, PUT' always;
        add_header 'Access-Control-Allow-Headers' '*' always;
        add_header 'Access-Control-Expose-Headers' 'Content-Length, Content-Range' always;

        if ($request_method = 'OPTIONS') { return 204; }
    }

    # 404 页面
    location = /static-404 {
        root /var/www/emby-404;
        try_files /cyber-404.html =404;
    }

    location / { rewrite ^ /static-404 last; }
}
